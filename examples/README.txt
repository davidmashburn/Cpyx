c_example shows a simple wrapping of c code

numpy_c_example shows an example of passing a pointer to a numpy arrays to a c function.


Each example can be run with this command:

python compile_and_run.py
